---
- name: Install WordPress, MariaDB, Nginx, and PHP-FPM
  hosts: *
  remote_user: node
  # remote_user: user
  # sudo: yes

  roles:
    - common
    - mariadb
    - nginx
    - php-fpm
    - wordpress
